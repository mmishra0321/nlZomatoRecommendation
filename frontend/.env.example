# Browser should call the Next.js API route.
NEXT_PUBLIC_API_BASE_URL=/api/recommendations

# Server-side proxy target (set this in Vercel to your Render backend URL).
BACKEND_API_BASE_URL=http://127.0.0.1:8000
